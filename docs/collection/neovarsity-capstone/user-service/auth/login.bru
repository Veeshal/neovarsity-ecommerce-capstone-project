meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{base-url}}{{context}}/v1/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "vishal@abc.com",
    "password": "4321"
  }
}

tests {
  let token = res.body.accessToken;
  bru.setVar("access-token", token);
  
}

meta {
  name: Update Profile
  type: http
  seq: 8
}

put {
  url: {{base-url}}{{context}}/v1/users
  body: json
  auth: inherit
}

body:json {
  {
    "name": "string",
    "phone": "string"
  }
}

settings {
  encodeUrl: true
}
